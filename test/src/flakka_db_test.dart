// ignore_for_file: prefer_const_constructors
import 'package:flakka_db/flakka_db.dart';
import 'package:test/test.dart';

void main() {
  group('FlakkaDb', () {
    test('can be instantiated', () {
      expect(FlakkaDb(), isNotNull);
    });
  });
}
